# todo have bug... 这里要做的是把order模块的配置文件通过配置中心从git上拉下来,而不是在这里写.....
spring:
  cloud:
    config:
      discovery:
        enabled: true
        service-id: CMS-CONFIG
      uri: http://localhost:8083/
      label: master
      profile: dev
  application:
    name: cms-order